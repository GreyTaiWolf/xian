{
  "name": "cangming",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit && vite build",
    "selftest": "vite build --config vite.selftest.config.ts && node dist-test/selftest.mjs && vite build --config vite.worldsim.config.ts && node dist-worldsim/worldsim-selftest.mjs",
    "worldsim:selftest": "vite build --config vite.worldsim.config.ts && node dist-worldsim/worldsim-selftest.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vite": "^7.0.0"
  }
}
